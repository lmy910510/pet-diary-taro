---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-02-25T11:12:05.505Z
provider: 
---

# UI 视觉风格规范

## 图标使用

- **禁止使用 Emoji 作为 UI 图标**，Emoji 在不同平台渲染不一致，且风格难以统一
- 需要图标时，**统一使用 Lucide 图标库**（已安装 `lucide-vue-next`）
- 图标风格：**线形（outline/stroke）**，stroke-width 推荐 `1.5`
- 图标大小根据上下文选择：小按钮/标签 `16px`，普通按钮 `20px`，大图标 `24px`+

## 间距与尺寸：4 的倍数栅格

- **所有间距、尺寸值必须是 4 的倍数**（4rpx、8rpx、12rpx、16rpx、20rpx、24rpx、28rpx、32rpx、36rpx、40rpx ...）
- 适用范围：`padding`、`margin`、`gap`、`width`、`height`、`border-radius`、`top/right/bottom/left`
- 禁止使用非 4 倍数的任意值（如 `5rpx`、`13rpx`、`15rpx`、`22rpx`）
- 唯一例外：`border-width`（可用 `2rpx`、`4rpx`）和 `1px` 细线

```scss
// 正确
padding: 16rpx 24rpx;
gap: 12rpx;
border-radius: 36rpx;
width: 72px;

// 错误
padding: 15rpx 25rpx;
gap: 10rpx;
border-radius: 30rpx;
width: 70px;
```

## 圆角风格

- 使用 **大圆角、圆润风格**
- 优先使用设计变量：`var(--radius)`、`var(--radius-lg)`、`var(--radius-full)`
- 按钮、输入框、卡片等组件保持圆角一致性

### 嵌套圆角规则（强制）

嵌套圆角是本项目最重要的视觉规则之一，**每次编写包含子元素的容器样式时必须检查**。

**核心公式**：`子圆角 = 父圆角 - 子元素到父容器边缘的距离`

**步骤**：
1. 确定父容器的 `border-radius`
2. 计算子元素到父容器对应边缘的实际距离（padding + margin）
3. 用公式计算子元素圆角
4. 结果向最近的 4 倍数取整

**规则细节**：
- 子元素圆角计算结果 < 8rpx 时，直接设为 `8rpx`（保持最小圆角感）
- 禁止在圆角容器内使用 `border-radius: 9999px`（药丸形），因为无法与父容器弧线平行
- 禁止在圆角容器内使用 `border-radius: 0`（直角），除非子元素完全不靠近容器圆角区域
- 如果子元素有多个嵌套层级，逐级计算：A → B → C，每级都要用公式

```scss
// 正确：逐级嵌套
.input-box {
  border-radius: 36rpx;
  padding: 12rpx;                 // 子元素到边缘 = 12rpx

  .toolbar {
    // 不需要独立圆角（无背景色/边框的纯布局层可跳过）
    padding: 0;

    .button {
      border-radius: 24rpx;      // 36 - 12 = 24rpx ✓
    }
  }
}

// 正确：toolbar 有背景色时需要圆角
.input-box {
  border-radius: 36rpx;
  padding: 16rpx;

  .toolbar {
    border-radius: 20rpx;        // 36 - 16 = 20rpx ✓
    background: #FAFAF9;
    padding: 8rpx;

    .button {
      border-radius: 12rpx;      // 20 - 8 = 12rpx ✓
    }
  }
}

// 错误：子元素圆角未计算
.input-box {
  border-radius: 36rpx;
  padding: 12rpx;

  .button {
    border-radius: 12rpx;        // 应该是 24rpx ✗
  }
}

// 错误：药丸形破坏嵌套
.card {
  border-radius: 20rpx;
  padding: 8rpx;

  .tag {
    border-radius: 9999px;        // 弧线无法平行 ✗
  }
}
```

### 实践检查清单

编写样式时逐项检查：
- [ ] 容器有 `border-radius` 吗？→ 子元素必须计算嵌套圆角
- [ ] 所有数值都是 4 的倍数吗？
- [ ] 子元素圆角 + 到边缘距离 = 父圆角吗？
- [ ] 有没有使用 `9999px` 药丸圆角？→ 改为计算值
- [ ] 纯布局层（无背景/边框）可以跳过圆角，但子元素仍然要算到最外层有圆角的容器

## 边框与投影

- **优先使用投影替代描边**，投影更柔和精致
- 轻边框效果：`box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.04)`
- 卡片投影：`box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.04), 0 4rpx 16rpx rgba(0, 0, 0, 0.05), 0 8rpx 32rpx rgba(0, 0, 0, 0.03)`
- 如果必须用描边，使用 `4rpx` 宽度，颜色用 `#E7E5E4` 或 `var(--border)`
- 禁止在输入框/卡片等大面积组件上使用粗灰色描边

## 配色规范

- 严格遵循已引入的设计规范和组件库变量
- 主色：`var(--primary)`、`var(--primary-foreground)`
- 前景/背景：`var(--foreground)`、`var(--background)`、`var(--muted)`
- 边框：`var(--border)`、`var(--ring)`
- 禁止硬编码颜色值，除非是特殊场景（如渐变、警告色等）

## 交互反馈

- 按钮/可点击元素加 `transition: all 0.2s ease`
- 按压反馈：`&:active { transform: scale(0.92~0.96); }`
- 小按钮缩放 `0.92`，大按钮/卡片缩放 `0.96`

## 示例

```vue
<!-- 正确：使用 Lucide 图标 -->
<script setup>
import { Scale, AlertCircle } from 'lucide-vue-next'
</script>
<template>
  <Scale :size="16" :stroke-width="1.5" />
</template>

<!-- 错误：使用 Emoji -->
<template>
  <span>⚖️</span>
</template>
```